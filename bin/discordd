#!/bin/bash

if [[ "$HTTP_PROXY" == "" ]]; then
    MY_PROXY="socks5://192.168.1.20:1082"
else
    MY_PROXY="$HTTP_PROXY"
fi

if [[ `uname` == Linux ]]; then
    daemonize $(which discord) --proxy-server="$MY_PROXY"
fi
